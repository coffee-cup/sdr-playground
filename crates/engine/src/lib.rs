//! The runtime. Assembles a `Source`, channels, decoders, mixer, and event bus into a
//! running system, owns the threading model, and exposes a control surface. UI-agnostic,
//! with IO injected. `app` and `cli` are interchangeable consumers. See `docs/ARCHITECTURE.md`.
//!
//! Intentionally empty until the pipeline lands.
