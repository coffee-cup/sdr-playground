//! `Source` implementations: the RTL-SDR driver, the file replay source, and the
//! pre-trigger ring. The only crate that touches hardware. See `docs/ARCHITECTURE.md`.
//!
//! Intentionally empty until the pipeline lands.
