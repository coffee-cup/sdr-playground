//! Headless front-end and decoder test harness. A peer of `app` over the same `engine`,
//! which is what keeps the core free of any UI dependency. See `docs/ARCHITECTURE.md`.

fn main() {
    println!("sdr: headless pipeline harness — not yet implemented. See docs/ARCHITECTURE.md");
}
